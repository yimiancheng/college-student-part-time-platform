<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dlut.platform.dao.util.CommandDao">
    <update id="executor" parameterType="java.lang.String">
        ${content}
    </update>
    <select id="query" parameterType="java.lang.String" resultType="java.util.Map">
        ${content}
    </select>
    <insert id="add" parameterType="java.lang.String">
        ${content}
    </insert>
    <delete id="remove" parameterType="java.lang.String">
        ${content}
    </delete>
</mapper>